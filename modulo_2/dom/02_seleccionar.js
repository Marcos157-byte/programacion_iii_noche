const titulo=document.getElementById("title");
console.log(litutlo,textComent);
const notas = documentos.getElementById('nota')
Array.from(notas)
    .forEach(nota=>console.log(nota.textComent));
const items = document.getElementsByClassName('item');
Array.form(items)
.forEach(item=>console.log(item.textComent));
const notaNames=document.getElementsByName('notaName');
Array.from(notaNames)
        .forEach(nota=>console.log(nota.textContent));